Hi [ISD Analyst],

As part of the Bitbucket DR commissioning activities, we completed end-to-end testing of LDAP authentication.

Following the previous firewall implementation on TCP/636 (LDAPS), authentication was unsuccessful despite further troubleshooting, including certificate renewal and validation.

To verify the configuration, a temporary firewall rule was implemented using **TCP/389**, which matches the existing Bitbucket Production LDAP configuration (SSL/TLS disabled). Authentication was successful immediately after applying the temporary rule.

Based on the testing completed, the DR environment requires the same LDAP connectivity as the current Production environment. This requirement also applies to the planned Bitbucket Production Node 2 deployment to ensure configuration consistency.

Kindly review and implement the firewall configuration to allow LDAP connectivity over **TCP/389**, in line with the existing Production configuration. A screenshot of the Production Bitbucket LDAP user directory configuration is attached for reference.

Please let us know if any further information is required.

Kind regards,

Ibrahim
