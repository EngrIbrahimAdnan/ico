**SCA Python Dependency Investigation Update**

Reviewed the reported SCA findings related to Python dependencies (`djangocms-text`, `djangocms-versioning`, `requests-file`).

**Findings:**

* Confirmed the application repository contains `requirements.txt` with Python dependencies.
* Confirmed the Jenkins build stage creates a Python virtual environment (`website-env`) and installs dependencies successfully using `pip install -r requirements.txt`.
* Verified the installed Python packages are present in the Jenkins workspace under `website-env`.
* Reviewed the generated Dependency-Check report; the reported Python packages were not found in the Jenkins-generated SCA report.

**Current Assessment:**

* Python dependencies are being prepared correctly during the build process.
* Further validation is required to confirm whether the Dependency-Check scan configuration is analysing the Python environment/dependencies.
* The manual Dependency-Check Docker command provided by Security appears to use a different execution method and requires comparison with the Jenkins scan configuration.

**Next Action:**

* Align with Security team to confirm the expected SCA scanning approach and compare the manual scan configuration against the Jenkins Dependency-Check implementation before making pipeline changes.
