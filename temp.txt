Hi Team,

As part of our investigation, we compared the behavior between the Pre-Production and Production environments.

During validation, we observed that the `curl` request executed against the Production Deployment Manager returned `null` values for both the authentication token and deployment ID. The same request executed against the Pre-Production Deployment Manager returned valid values for both fields.

This difference has been identified as part of the ongoing investigation, and we are continuing to determine why the expected values are not being returned in the Production environment. We will provide a further update once additional findings are available.

Kind regards,
