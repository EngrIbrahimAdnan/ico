**Progress Update:**

* Validated firewall connectivity between the Bitbucket DR server and the Production database, resolving the initial application startup issue.
* Confirmed successful startup of the Bitbucket DR application and verified access to the login page.
* Investigated LDAP authentication and validated that the Production environment uses LDAP over TCP/389 (SSL/TLS disabled). Following implementation of a temporary firewall rule on TCP/389, LDAP authentication was successfully validated on the DR environment.
* Identified an HTTPS certificate issue whereby the Bitbucket DR server presents a certificate containing the Production hostname under the Subject Alternative Name (SAN), resulting in the browser reporting the DR URL as **"Not Secure."**
* Performed analysis of the Bitbucket HTTPS configuration and confirmed the issue is related to the certificate presented by the DR server.
* Engaged the relevant support team to request the appropriate HTTPS certificate for the Bitbucket DR hostname. This is the remaining blocker to completing the Bitbucket DR commissioning activities.
